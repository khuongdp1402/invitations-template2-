---
wave: 1
depends_on: []
files_modified:
  - "package.json"
  - "components/canvas/Gallery3D.jsx"
  - "components/sections/GallerySection.jsx"
  - "components/sections/TimelineSection.jsx"
  - "components/sections/HeroSection.jsx"
autonomous: true
---

# Phase 04.1.1: Nâng cấp hoạt ảnh 3D và giao diện động học

<objective>
Install Three.js/React Three Fiber libraries, implement Gallery3D component, update TimelineSection with GSAP MotionPath, and redesign HeroSection with an 80-20 layout and SVG handwriting.
</objective>

<must_haves>
- `@react-three/fiber`, `@react-three/drei`, `three` in package.json
- `Gallery3D.jsx` using `<Canvas>` and `ScrollControls`
- `TimelineSection.jsx` using `MotionPathPlugin`
- `HeroSection.jsx` using flex/grid for 80-20 layout
</must_haves>

## Tasks

<task>
<id>04.1.1-01</id>
<title>Install Three.js and GSAP Plugins</title>
<read_first>
- package.json
</read_first>
<action>
Install `@react-three/fiber`, `@react-three/drei`, and `three` by running `npm install @react-three/fiber @react-three/drei three`.
Update `package.json` with these dependencies. Ensure gsap is already present.
</action>
<acceptance_criteria>
- `package.json` contains `"@react-three/fiber"`
- `package.json` contains `"@react-three/drei"`
- `package.json` contains `"three"`
</acceptance_criteria>
</task>

<task>
<id>04.1.1-02</id>
<title>Create Gallery3D Component</title>
<read_first>
- .planning/phases/04.1.1-n-ng-c-p-ho-t-nh-3d-v-giao-di-n-ng-h-c-three-js-gsap/04.1.1-RESEARCH.md
- components/sections/GallerySection.jsx
</read_first>
<action>
Create file `components/canvas/Gallery3D.jsx`.
Import `Canvas` from `@react-three/fiber` and `ScrollControls`, `Scroll`, `Image` from `@react-three/drei`.
Export a default component `Gallery3D` that renders a `<Canvas>` wrapping `<ScrollControls pages={3}>` and a `<Scroll>` area that contains a few `<Image>` elements positioned in 3D space (`position={[x, y, z]}`).
Then, update `components/sections/GallerySection.jsx` to import and render `<Gallery3D />` instead of the static grid. Apply an absolute or full-screen layout to the container so the canvas fits.
</action>
<acceptance_criteria>
- `components/canvas/Gallery3D.jsx` contains `from '@react-three/fiber'`
- `components/canvas/Gallery3D.jsx` contains `from '@react-three/drei'`
- `components/sections/GallerySection.jsx` contains `import Gallery3D`
- `components/sections/GallerySection.jsx` contains `<Gallery3D`
</acceptance_criteria>
</task>

<task>
<id>04.1.1-03</id>
<title>Update TimelineSection with MotionPathPlugin</title>
<read_first>
- .planning/phases/04.1.1-n-ng-c-p-ho-t-nh-3d-v-giao-di-n-ng-h-c-three-js-gsap/04.1.1-RESEARCH.md
- components/sections/TimelineSection.jsx
</read_first>
<action>
Modify `components/sections/TimelineSection.jsx`.
Import `MotionPathPlugin` from `gsap/MotionPathPlugin`.
Update `gsap.registerPlugin(ScrollTrigger, MotionPathPlugin);`.
Create a visible SVG path representing the timeline wire (e.g. `<path id="timelinePath" d="M 50 0 Q 80 200 50 400 T 50 800" fill="transparent" stroke="#D4AF37" />`).
Add a small SVG car or heart icon (e.g. `<circle id="timelineIcon" r="10" fill="#D4AF37" />`).
In `useGSAP`, animate the icon along the path:
`gsap.to("#timelineIcon", { motionPath: { path: "#timelinePath", align: "#timelinePath", alignOrigin: [0.5, 0.5] }, scrollTrigger: { trigger: sectionRef.current, scrub: true, start: "top center", end: "bottom center" } });`
</action>
<acceptance_criteria>
- `components/sections/TimelineSection.jsx` contains `gsap/MotionPathPlugin`
- `components/sections/TimelineSection.jsx` contains `MotionPathPlugin`
- `components/sections/TimelineSection.jsx` contains `motionPath:`
</acceptance_criteria>
</task>

<task>
<id>04.1.1-04</id>
<title>HeroSection Redesign (80-20 & Handwriting)</title>
<read_first>
- .planning/phases/04.1.1-n-ng-c-p-ho-t-nh-3d-v-giao-di-n-ng-h-c-three-js-gsap/04.1.1-RESEARCH.md
- components/sections/HeroSection.jsx
</read_first>
<action>
Modify `components/sections/HeroSection.jsx`.
Change the layout to a Flex or Grid for the 80-20 split. e.g. `<div className="flex w-full h-full">`.
Left side (80% or `w-4/5`): `<Image>` component with `object-fit: cover` and `object-position: left center`.
Right side (20% or `w-1/5`) or positioned overlay (for mobile): Apply glassmorphism `bg-white/30 backdrop-blur-md`. Place the "Save the Date" block here.
Below the bride and groom names, add an SVG `<path>` (e.g. a cursive scribble) with `strokeDasharray` and `strokeDashoffset` setup.
In `useGSAP`, animate `strokeDashoffset` to 0 to simulate handwriting when loaded.
</action>
<acceptance_criteria>
- `components/sections/HeroSection.jsx` contains `object-position`
- `components/sections/HeroSection.jsx` contains `strokeDasharray`
- `components/sections/HeroSection.jsx` contains `backdrop-blur`
</acceptance_criteria>
</task>

## Verification
- Run `npm run build` and ensure no compilation errors.
- Run `npm ls @react-three/fiber` to ensure library is installed.
- Check that the `04.1.1-UI-SPEC.md` colors (`#D4AF37`) are being used in `TimelineSection` and `HeroSection`.
